package com.aiso.loopreminder.ui.theme

import androidx.compose.ui.graphics.Color

/** Design tokens — mirrors the Ardot design file (循环提醒待办App). */
val Cream = Color(0xFFF8F6F2)        // app background
val Primary = Color(0xFF014DB2)      // primary blue
val Success = Color(0xFF10B981)      // done / completed
val Danger = Color(0xFFEF4444)       // overdue
val Amber = Color(0xFFF59E0B)        // in-progress / escalation
val TextPrimary = Color(0xFF0A1628)
val TextSecondary = Color(0xFF6B7280)
val Surface = Color(0xFFFFFFFF)      // cards
val Border = Color(0xFFEAEAEA)       // hairline border
